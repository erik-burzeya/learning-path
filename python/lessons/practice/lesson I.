✔️ TASK 1: Create a program that requests the following data:
- name
- age
- heigth (in m)
- student status (yes/no)

✔️ TASK 1.2: Change the unit of heigth to feet (ft), if the user types "yes".

✔️ TASK 2: Create a shopping calculator with the following criteria:
- ask the user
    - product name
    - price (e.g. 2.99)
    - quantity (e.g. 3)
- convert types correctly
    - price -> float
    - quantity -> int
- calculate
    - total prize = price x quantity
-output
    - example:
        "You bought 3 apples.
        Total price: 8.97€."
    ! Try this on purpose first: 
    price = input("Price: ")
    quantity = input("Quantity: ")
    total = price * quantity

✔️ TASK 2.1: Use a while loop to make it possible to add more items to the chart!

✔️ TASK 3: Grade evaluation
Ask the user for a score (0–100) and print the grade:
    90–100 → "Excellent"
    75–89 → "Good"
    50–74 → "Pass"
    below 50 → "Fail"
✔️ ➕ Extra Challenge:
If the score is exactly 100, print "Perfect score!"
Use 'and' for ranges

TASK 3.1: Access System

Ask for: 
    - age
    - has_ticket
    - is_vip

Rules:
Access is granted if:
    - age ≥ 18 AND has a ticket
OR
    - the user is VIP

➕ Extra Challenge:
- If age < 16 → always deny (use not or priority logic)
- Print different messages:
    - "Access granted"
    - "Access denied"
    - "VIP access granted"

Task 4: “Profile Manager”
Create a dictionary that represents a person and then manipulate it.
1. Create a dictionary person with at least:
- name
- age
- city
2. Print the person's name and age.
3. Increase the age by 1 (simulate a birthday).
4. Add a new key:
5. hobby
6. Change the city to a new one.
7. Print the final dictionary.